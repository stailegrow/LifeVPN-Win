# Life VPN для Windows

VPN-клиент для Windows: подписки, гибкая маршрутизация, замер задержки и
скорости — в одном окне. Точная копия мак-версии Life VPN 2.2.0: тот же
интерфейс, те же семь тем, та же логика подключения и маршрутизации.

---

## Что это

Life VPN поднимает VLESS-подключение через ядро
[Xray-core](https://github.com/XTLS/Xray-core) и включает системный прокси
Windows. Приложение работает по подписке: ссылку добавляют один раз, дальше
список узлов обновляется сам. Узлы, добавленные на панели, появляются в
клиенте, удалённые исчезают.

## Возможности

**Подключение**

- VLESS поверх TCP с Reality и XTLS Vision, а также транспорт XHTTP.
- Маршрутизация: правила задают, какие адреса идут через туннель, а какие
  напрямую («Обход РФ» и «Глобально»).
- Раздельный DNS: российские имена резолвит местный DNS, остальные — DNS через
  туннель.
- Обход локальной сети: принтеры, NAS и роутер остаются доступны при
  включённом VPN.
- Свой список доменов, которые всегда идут напрямую.
- QUIC блокируется явно.
- Системный прокси включается сразу для всех подключений, включая модем и VPN
  Windows. При выходе прежние настройки возвращаются. После аварийного
  завершения, выключения компьютера или выхода из учётной записи прокси тоже
  снимается.

**Подписки и узлы**

- Подписку можно добавить ссылкой, из буфера обмена (Ctrl+V в окне), QR-кодом
  с камеры или картинкой из файла.
- Список узлов обновляется при каждом запуске. Подписку можно переименовать,
  в ней видны счётчик трафика и срок.
- Отдельные `vless://` ссылки можно добавить вручную.

**Измерения**

- Задержка меряется напрямую от компьютера до узла и не зависит от того,
  включён VPN или нет.
- Фоновый опрос раз в десять секунд и мини-графики истории по каждому узлу.
- Замер скорости по трём независимым источникам.

**Интерфейс**

- Семь тем: пять светлых, мягкая тёмная и настоящий чёрный AMOLED.
- Живой фон и анимированная кнопка подключения.
- Значок в трее: зелёный, когда туннель поднят. Левый клик открывает окно,
  правый — меню.
- Русский и английский язык, переключается на месте.

## Установка

Запустите `LifeVPN-Setup-2.2.0.exe`. Права администратора не нужны: приложение
ставится в папку пользователя, ярлыки появляются на рабочем столе и в меню
«Пуск».

Установщик не подписан сертификатом, поэтому Windows SmartScreen при первом
запуске может показать предупреждение. Нажмите «Подробнее» → «Выполнить в
любом случае».

Закрытие окна крестиком не завершает приложение: оно остаётся в трее вместе с
поднятым туннелем, как на маке. Чтобы выйти, щёлкните значок в трее правой
кнопкой и выберите «Выйти».

Данные хранятся в `%APPDATA%\LifeVPN`: `servers.json`, `settings.json`, базы
правил и лог ядра.

## Сборка из исходников

Нужны Node.js 20+ и Python 3 с Pillow (только для иконок).

```bash
npm install
./scripts/fetch-xray.sh        # ядро Xray для Windows → resources/bin/xray.exe
npm run icons                  # icon.ico и значки трея
npm test                       # встроенные проверки
npm start                      # запуск без сборки
npm run dist                   # установщик и zip в dist/
```

Помощник системного прокси `resources/bin/lifevpn-proxy.exe` собирается из
`native/sysproxy.c`:

```bash
x86_64-w64-mingw32-gcc -O2 -municode -mconsole -s -o resources/bin/lifevpn-proxy.exe \
    native/sysproxy.c -lwininet -lrasapi32
```

Если собирать установщик не на Windows, нужен Wine (64- и 32-битный): им
electron-builder собирает деинсталлятор.

## Проверки

```bash
npm test
```

Сюда перенесены все 42 проверки мак-версии: разбор ссылок, сборка конфига
ядра, маршрутизация, DNS, форматы подписок, синхронизация и статистика
замеров. Добавлены проверки Windows-версии. Если рядом лежит ядро (переменная
`LIFEVPN_XRAY`), каждый собранный конфиг прогоняется ещё и через
`xray run -test`.

## Устройство

| Мак-версия (Swift)            | Windows-версия (Electron)            |
|-------------------------------|--------------------------------------|
| `Core/*.swift`, `Models/*`    | `src/core/*.js`                      |
| `Views/*.swift` (SwiftUI)     | `src/renderer/js/*.js` (Preact)      |
| `networksetup`                | `lifevpn-proxy.exe` (WinINet)        |
| значок в строке меню          | значок в трее                        |
| `SelfCheck.swift`             | `test/self-check.js`                 |

## Приватность

Приложение не собирает статистику и никуда не отправляет данные о
пользователе. Ссылки подписок, настройки и список узлов хранятся только на
этом компьютере. От себя клиент делает только такие сетевые запросы:
обновление подписки, загрузка баз маршрутизации и проверка внешнего адреса.

## Лицензия

MIT — см. [LICENSE](LICENSE).

Ядро [Xray-core](https://github.com/XTLS/Xray-core) поставляется отдельным
бинарником и распространяется под собственной лицензией (MPL-2.0).
